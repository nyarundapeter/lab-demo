"use client";
// Module: DWS.07.M02 — Imaging & Radiology
// Shell: SH.01  Journey stage: 1
// Layout: collection-grid
// Body-only page — TransactionShell provided by app/(authenticated)/layout.tsx
// Generated by scaffold-solution — do not edit (run with --force to regenerate)
import { useRouter } from "next/navigation";
import { CatalogueLayout } from "@dbp/shell-transaction/layouts";
import CatalogGrid from "@dbp/organisms/catalog-grid";

export default function ImagingRadiologyPage() {
  const router = useRouter();
  return (
      <CatalogueLayout
      >
        <CatalogGrid
          key="imaging-catalogue"
          {...({
        "entityType": "lab-service",
        "headline": "Imaging & Radiology.",
        "lede": "Ultrasound, CT, MRI, and X-Ray requests.",
        "filters": {
                "department": "imaging"
        },
        "cardFields": {
                "title": "name",
                "description": "summary",
                "typeLabel": "department",
                "status": "status",
                "statusTone": "success",
                "footerId": "code"
        },
        "filterFields": [
                {
                        "field": "status",
                        "label": "Status"
                }
        ]
} as Parameters<typeof CatalogGrid>[0])}
          onCardClick={(_t, id) => router.push("/marketplace/discern/" + encodeURIComponent(id))}
        />
      </CatalogueLayout>
  );
}
